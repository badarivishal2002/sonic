/**
 * Create Note Page
 * Page for creating new notes (text or voice)
 */

'use client';

import { NoteEditor } from '@/components/NoteEditor';

/**
 * Create Note Page Component
 * Client component that renders the note editor
 */
export default function CreateNotePage() {
  return <NoteEditor />;
}
